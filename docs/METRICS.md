# Metrics (skeleton)

Frozen-threshold evaluation: tau = 99th percentile of R0 train scores per
(seed, dataset, detector), frozen across codecs.

- point-F1, event-F1 (trailing window-to-point alignment), PR-AUC, VUS-PR (window 64)
- PA-F1 banned.

Details land in Task 2. No logic yet — skeleton only.
